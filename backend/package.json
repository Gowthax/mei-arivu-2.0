{
  "name": "mei-arivu-backend",
  "version": "1.0.0",
  "description": "Mei Arivu FastAPI Backend",
  "scripts": {
    "start": "venv\\Scripts\\uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "install-deps": "venv\\Scripts\\pip install -r requirements.txt",
    "setup": "venv\\Scripts\\pip install -r requirements.txt && venv\\Scripts\\python seed_db.py"
  }
}
